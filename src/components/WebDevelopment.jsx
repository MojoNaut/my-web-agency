import React from 'react';
import './WebDevelopement.css';

const WebDevelopement = () => {
    return (
        <section className="cta">
            <h2>we build site that convert</h2>
            <a href="/contact" className="btn">Contact Us</a>
        </section>
    );
}

export default WebDevelopement;
